node_modules/
.next/
out/
build/
.data/
.dsh-tools/
*.tsbuildinfo
next-env.d.ts
.DS_Store
Thumbs.db

# Секреты: любые .env*, кроме публичного шаблона.
# Создаётся Vercel CLI (.env.local, .env.neon.local) — в репозиторий не попадает.
.env*
!.env.example

# Привязка к проекту Vercel (содержит id проекта, но не секреты)
.vercel

# PDF-файлы в репозиторий не попадают (иллюстрации страниц — в JPG, они отслеживаются)
*.pdf
